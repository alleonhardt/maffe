[build-system]
requires = [
  "meson>=1.3.0",
  "meson-python>=0.16",
  "ninja",
  "pybind11>=2.11",
]
build-backend = "mesonpy"

[project]
name = "maffe"
version = "0.1"
description = "Python bindings for the maffe solver"
requires-python = ">=3.11"
license = "MIT"
